# Roadmap

This roadmap tracks the development phases for turning the static user patch into a fully customizable KOReader plugin (or an advanced patch with interactive settings).

## Phase 1: Dynamic Settings & Styles ✅ (shipped in v2.1.0)
*Context: Based on architectural research, the patch currently hardcodes a single "Floating Card" layout. We can expand this into multiple selectable styles by hooking into KOReader's menu system.*

- [x] **Menu Integration:** Hook into the settings menu (v2.1.1: the entry lives at the bottom of the Settings tab as **Banner style**).
  - *Implemented:* the patch wraps `setUpdateItemTable` on `ReaderMenu` and `FileManagerMenu` and registers a top-level `banner_style` key in the menu order table *before* the original builder runs; MenuSorter silently drops any item missing from the order, so this is the only reliable injection point. Same approach as the community ui-font patch. (KOReader loads plugin files with `dofile`, so monkey-patching the plugin module itself is unreliable; the menu modules are `require`-loaded core modules, so patching them sticks.)
- [x] **Style Switcher UI:** **Settings → Banner style → Message style**, radio-style picker for the five looks, persisted in `G_reader_settings` (`screensaver_banner_style`). Applies from the next sleep, no restart. Falls back to `B_SETT.style` when no menu choice has been made. (Bonus, v2.1.1: per-role **font pickers** under Banner style → Fonts.)
- [x] **Implement 'Floating Card' Style:** Default. Behaviour unchanged from v2.0.3.
- [x] **Implement 'Pill / Badge' Style:** Radius computed from the assembled card's height (`radius = height / 2`); the drop shadow follows the same radius.
- [x] **Implement 'Full-Width' Style:** Card stretched to `screen_w` via a `LeftContainer`, zero margins, square corners, no shadow; text uses all the width the chrome leaves over (`max_width_hl_*` percentages don't apply in this style).
- [x] **Implement 'Outlined' Style:** User background and corner radius kept, thick 5 px border, drop shadow removed. *(Corrected 2026-09-13: the box said 3 px; v2.1.0 thickened the border to 5 px and the code ships 5.)*
- [x] **Implement 'Bracketed' Style:** shipped in v2.1.0 as typographic rules above/below the text; redesigned in v2.1.2 as **"Flat box"** (menu label): a plain solid backing behind the text, no border, corners or shadow, which reads far better over real covers. Settings id unchanged.

## Phase 2: Robust Highlight Handling ✅ (shipped in v2.1.0)
*Context: Refining the aesthetic behavior when edge cases in book metadata or reading history occur.*

- [x] **Hide if N/A:** The highlight build is now guarded with `if highlightEnabled and random_highlight and random_highlight ~= ""`. Books with no eligible highlights (or any highlight that trims to an empty string) skip the whole section (accent line, quote, footer) instead of rendering an empty accent line with a blank space next to it.

## Testing

`test/harness.lua` stubs KOReader's widget modules and exercises the patch headlessly: order-table menu registration, style persistence and fallback, `UIManager:show` pass-through guards, per-style card assembly with layout assertions, and draw-time font resolution (70 checks). Run it under **both** Lua 5.4 and LuaJIT: `lua test/harness.lua` (CI does the same on every push). On-device visual verification per style is still worthwhile.

### Final audit 2026-09-14 (THE FINAL AUDIT: NEW findings, one line each; full detail in audit-final/2-kobo-style-sleepscreen-banner-prettified/FINAL-REPORT.md)

Combined-lens pass (the mission's one-file-preset deviation) at 717f385. Tally: 0 HIGH / 8 MEDIUM / 13 LOW + 2 S feature suggestions. NEW findings LOGGED, never executed:

- [x] [MEDIUM] Three KOReader-API assumptions to arbitrate against /mnt/us/koreader/frontend/ (spec 7 rule), fold into a v2.1.4: `strikethrough` is not a KOReader drawer name (`strikeout` is), so the allowed_hl_styles key never matches (lua:77); `lastfile` can be nil when sleeping from the file manager with banner wallpaper configured, putting getDocSettings(nil) + Sidecar:flush() on a crash path (lua:540-544); the wallpaper guard promises "Custom image" but the enum accepts only cover/document_cover/random_image (README:40, lua:507-526). *(All three arbitrated against the mounted device's v2026.07.2 sources on 2026-09-14 and executed in v2.1.4: `strikeout` rename shipped with filter-matching tests; `DocSettings:open(nil)` confirmed a hard error (crashes in getSidecarFilename) and the sidecar is now skipped when lastfile is unset; the enum confirmed CORRECT as-is, `document_cover` is KOReader's "custom image or cover" and `image_file` was migrated away upstream in 2024-04, so only the README wording and the code comment were aligned.)*
- [x] [MEDIUM] Doc pass: spec.md:3 "through v2.1.2" is stale (v2.1.3 shipped); README:25's "compatibility shim for versions older than v2025.04-115" vs spec's "2024.07+" with no version-conditional code in the patch; 24 em-dash lines across the prose docs + one shipped help string (lua:165) vs the house no-em-dash rule (the v2.1.3 entries are already clean; one old-copy sweep; roadmap checkmarks and the Lua's box-drawing art exempt). *(Executed 2026-09-14: the 24 prose lines recast in a docs-only commit, the Lua:165 help string in v2.1.4, README:25 now names what actually degrades instead of a version number, spec status line tracks the release.)*
- [x] [MEDIUM] The v2.1.3 tag is an incomplete catch-up tag: previous tag is v2.0.2-vir.1, so by the house rule it should carry every entry since v2.0.3, not only the newest (verify push state online; force-push rules apply); confirm the GitHub Release exists verbatim. *(Executed 2026-09-14 per Brandon's explicit go: push state verified first (both tags on origin), tag recut carrying the v2.0.3-v2.1.3 entries verbatim, force-pushed, and the GitHub Release recreated from it.)*
- [x] [MEDIUM] roadmap.md:24 says the harness runs "56 checks"; actual is 58 ok() sites (v2.1.3's T7 added two after the claim). Update. *(Executed 2026-09-14: the count tracks reality again, 70 after v2.1.4 added the T3 type-guard case, the stale-font fallback test, the strikeout matching tests, the reset-entry and alphabetical-sort tests, and the no-lastfile guard test.)*
- [x] [LOW] math.random unseeded: LuaJIT repeats its sequence per boot unless KOReader seeds globally, making the "random" highlight identical across reboots (lua:640); parseFooterText guards Sidecar twice then dereferences unguarded (lua:441-447); self.ui written onto the UIManager singleton persists after wake (lua:542); a stale menu font path skips the still-valid B_SETT fallback (lua:211-216); the unexplained gettext-shaped identity stub (lua:116); the stale pre-v2.1.1 menu location comment (lua:112). *(All six executed in v2.1.4: explicit randomseed, one guarded posture for all three Sidecar uses, patch-local ui_instance, three-candidate font fallback chain with a harness test, gettext stub explained in place, menu-location comment updated.)*
- [x] [LOW] spec.md:30 font-chain wording (footer/highlight roles end at NotoSerif, not cfont); RESEARCH_STYLES.md needs a one-line historical banner (its "3-5px" example is the origin of the 3 px confusion); CLAUDE.md:60 still names .clinerules; harness T3 lacks a wrong-screensaver_type case (lua:523-526 is the third guard). *(All four executed 2026-09-14: spec font chain rewritten per role for the v2.1.4 fallback behavior, historical banner added, .clinerules reference replaced with the AGENTS.md symlink note, T3 type-guard case added.)*
- [x] [LOW] GitHub: optional 10-line Actions workflow running the harness under both Lua 5.4 and LuaJIT (spec 7 already demands both); v2.0.2-vir.1 is a one-line paraphrase tag (historical; fix opportunistically). *(Executed 2026-09-14 per Brandon's go: `.github/workflows/harness.yml` added in v2.1.4. The v2.0.2-vir.1 paraphrase tag stays as record: rewriting it buys nothing user-visible and its message carries the fork-provenance note.)*
- [x] [FEATURE] S: a "Default (from the config file)" reset entry for Message style, mirroring the font roles' reset (lua:243-277; today a menu style pick cannot be undone without hand-editing settings). S: sort the font picker alphabetically (lua:279-298). *(Both executed in v2.1.4 per Brandon's go, each with harness coverage.)*

CONFIRMED-prior CLOSED: the 3px/5px roadmap finding (717f385's dated ship note matches border_size = 5 everywhere it is claimed). Verified clean under the fine-toothed comb: the menu-injection wrap, persistence fallbacks, the v2.1.3 gsub-literal fix, pill/full-width/shadow math, no Lua 5.4/LuaJIT divergent syntax, all style/settings ids consistent across README/spec/code. Slop-reader note (inline in the combined pass): voice human, checkmarks convention; the 24-line dash sweep is the one mechanical finding.
